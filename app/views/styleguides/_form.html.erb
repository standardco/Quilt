<%= form_for(@styleguide) do |f| %>
  <% if @styleguide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@styleguide.errors.count, "error") %> prohibited this styleguide from being saved:</h2>

      <ul>
      <% @styleguide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :tags %><br>
    <%= f.text_area :tags %>
  </div>
    <div class="field">
    <%= f.label :is_public %><br>
    <%= f.text_field :is_public %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
